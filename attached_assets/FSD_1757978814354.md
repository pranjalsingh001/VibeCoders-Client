# 🎨 Frontend Specification Document (FSD) - VibeCoders IDE

## 📋 Table of Contents
1. [Project Overview](#project-overview)
2. [API Documentation](#api-documentation)
3. [User Flows & Pages](#user-flows--pages)
4. [Design System & UI Kit](#design-system--ui-kit)
5. [Authentication & State Management](#authentication--state-management)
6. [WebSocket/Real-time Requirements](#websocketreal-time-requirements)
7. [Test Credentials & Dummy Data](#test-credentials--dummy-data)
8. [Expected Deliverables](#expected-deliverables)
9. [Technical Stack](#technical-stack)
10. [Development Guidelines](#development-guidelines)

---

## 🎯 Project Overview

### What is VibeCoders IDE?
An AI-powered IDE that simulates real-world software development workflows. Users act as project managers while AI acts as a developer, guiding from idea to planning to fully coded, scalable full-stack applications.

### Frontend Goals
- Create an intuitive, VS Code-like interface
- Provide seamless AI interaction experience
- Enable real-time collaboration and file editing
- Support project planning and execution workflows

---

## 🔌 API Documentation

### Base URL
```
http://localhost:5000/api/v1
```

### Required Headers
```javascript
{
  "Content-Type": "application/json",
  "Authorization": "Bearer <JWT_TOKEN>"
}
```

### 1️⃣ Authentication Endpoints

#### POST `/auth/signup`
**Purpose**: Register new user

**Input JSON**:
```json
{
  "username": "johndoe",
  "email": "johndoe@example.com",
  "password": "securepassword123"
}
```

**Output JSON**:
```json
{
  "success": true,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "_id": "68a2ab2404c0d9f324ed5a9e",
    "username": "johndoe",
    "email": "johndoe@example.com",
    "createdAt": "2024-01-15T10:30:00.000Z"
  }
}
```

#### POST `/auth/login`
**Purpose**: User login

**Input JSON**:
```json
{
  "email": "johndoe@example.com",
  "password": "securepassword123"
}
```

**Output JSON**:
```json
{
  "success": true,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "_id": "68a2ab2404c0d9f324ed5a9e",
    "username": "johndoe",
    "email": "johndoe@example.com"
  }
}
```

### 2️⃣ Project Management Endpoints

#### POST `/projects/create`
**Purpose**: Create new project

**Input JSON**:
```json
{
  "name": "Twitter Clone",
  "idea": "Build a social media platform similar to Twitter with real-time updates"
}
```

**Output JSON**:
```json
{
  "success": true,
  "project": {
    "_id": "68a2ab2404c0d9f324ed5a9e",
    "name": "Twitter Clone",
    "idea": "Build a social media platform similar to Twitter with real-time updates",
    "status": "clarifying",
    "createdAt": "2024-01-15T10:30:00.000Z",
    "userId": "68a2ab2404c0d9f324ed5a9e"
  }
}
```

#### GET `/projects`
**Purpose**: List user projects

**Output JSON**:
```json
{
  "success": true,
  "projects": [
    {
      "_id": "68a2ab2404c0d9f324ed5a9e",
      "name": "Twitter Clone",
      "status": "coding",
      "createdAt": "2024-01-15T10:30:00.000Z"
    }
  ]
}
```

#### GET `/projects/:id`
**Purpose**: Get project details

**Output JSON**:
```json
{
  "success": true,
  "project": {
    "_id": "68a2ab2404c0d9f324ed5a9e",
    "name": "Twitter Clone",
    "idea": "Build a social media platform similar to Twitter with real-time updates",
    "status": "coding",
    "blueprint": { /* blueprint data */ },
    "designDocs": [ /* HLD/LLD documents */ ],
    "createdAt": "2024-01-15T10:30:00.000Z"
  }
}
```

### 3️⃣ Planning Endpoints

#### POST `/planning/clarify`
**Purpose**: Get AI clarification questions

**Input JSON**:
```json
{
  "projectId": "68a2ab2404c0d9f324ed5a9e",
  "projectName": "Twitter Clone",
  "ideaDescription": "Build a social media platform similar to Twitter with real-time updates"
}
```

**Output JSON**:
```json
{
  "success": true,
  "questions": [
    "What authentication method would you prefer? (JWT, OAuth, etc.)",
    "Do you want real-time features like live notifications?",
    "What database would you prefer? (MongoDB, PostgreSQL, etc.)",
    "Should users be able to upload images/videos?"
  ],
  "projectId": "68a2ab2404c0d9f324ed5a9e"
}
```

#### POST `/planning/clarify/answer`
**Purpose**: Submit clarification answers

**Input JSON**:
```json
{
  "projectId": "68a2ab2404c0d9f324ed5a9e",
  "answers": [
    "JWT authentication",
    "Yes, real-time notifications",
    "MongoDB for flexibility",
    "Yes, image uploads only"
  ]
}
```

**Output JSON**:
```json
{
  "success": true,
  "message": "Answers saved successfully"
}
```

### 4️⃣ Blueprint Endpoints

#### POST `/blueprint/generate`
**Purpose**: Generate project blueprint

**Input JSON**:
```json
{
  "projectId": "68a2ab2404c0d9f324ed5a9e"
}
```

**Output JSON**:
```json
{
  "success": true,
  "blueprint": {
    "_id": "68a2ab2404c0d9f324ed5a9e",
    "content": {
      "architecture": "Full-stack MERN application",
      "techStack": {
        "frontend": "React with TypeScript",
        "backend": "Node.js with Express",
        "database": "MongoDB",
        "realTime": "Socket.IO"
      },
      "features": ["User authentication", "Real-time posts", "Image uploads"],
      "folderStructure": {
        "client": ["src", "public", "package.json"],
        "server": ["controllers", "models", "routes", "middleware"]
      }
    },
    "versions": [1],
    "createdAt": "2024-01-15T10:30:00.000Z"
  }
}
```

### 5️⃣ Design Endpoints

#### POST `/design/hld`
**Purpose**: Generate High-Level Design

**Input JSON**:
```json
{
  "projectId": "68a2ab2404c0d9f324ed5a9e",
  "techPref": "MERN stack"
}
```

**Output JSON**:
```json
{
  "success": true,
  "hld": {
    "content": "# High-Level Design\n\n## System Architecture\n- Frontend: React SPA\n- Backend: Node.js API\n- Database: MongoDB\n- Real-time: Socket.IO",
    "diagrams": {
      "architecture": "mermaid diagram code",
      "dataFlow": "mermaid diagram code"
    }
  },
  "designDocId": "68a2ab2404c0d9f324ed5a9e"
}
```

#### POST `/design/lld`
**Purpose**: Generate Low-Level Design

**Input JSON**:
```json
{
  "projectId": "68a2ab2404c0d9f324ed5a9e",
  "techPref": "MERN stack"
}
```

**Output JSON**:
```json
{
  "success": true,
  "lld": {
    "content": "# Low-Level Design\n\n## Database Schema\n```javascript\nUser: { _id, username, email, password, avatar }\nPost: { _id, content, userId, imageUrl, createdAt }\n```",
    "apiSpecs": {
      "endpoints": ["POST /auth/login", "GET /posts", "POST /posts"],
      "models": "Detailed API documentation"
    }
  },
  "designDocId": "68a2ab2404c0d9f324ed5a9e"
}
```

### 6️⃣ File Operations Endpoints

#### POST `/files/write`
**Purpose**: Write project file

**Input JSON**:
```json
{
  "projectId": "68a2ab2404c0d9f324ed5a9e",
  "relativePath": "client/src/App.js",
  "content": "import React from 'react';\n\nfunction App() {\n  return <div>Hello World</div>;\n}\n\nexport default App;"
}
```

**Output JSON**:
```json
{
  "success": true,
  "message": "File written successfully",
  "filePath": "/projects/68a2ab2404c0d9f324ed5a9e/client/src/App.js"
}
```

#### GET `/files/read`
**Purpose**: Read project file

**Query Parameters**:
```
?projectId=68a2ab2404c0d9f324ed5a9e&relativePath=client/src/App.js
```

**Output JSON**:
```json
{
  "success": true,
  "content": "import React from 'react';\n\nfunction App() {\n  return <div>Hello World</div>;\n}\n\nexport default App;",
  "fullPath": "/projects/68a2ab2404c0d9f324ed5a9e/client/src/App.js"
}
```

#### GET `/files/list`
**Purpose**: List directory contents

**Query Parameters**:
```
?projectId=68a2ab2404c0d9f324ed5a9e&dir=client/src
```

**Output JSON**:
```json
{
  "success": true,
  "entries": [
    {
      "name": "App.js",
      "type": "file",
      "size": 1024
    },
    {
      "name": "components",
      "type": "directory"
    }
  ]
}
```

### 7️⃣ Execution Endpoints

#### POST `/exec/start`
**Purpose**: Start project container

**Input JSON**:
```json
{
  "projectId": "68a2ab2404c0d9f324ed5a9e"
}
```

**Output JSON**:
```json
{
  "success": true,
  "sessionId": "68a2ab2404c0d9f324ed5a9e",
  "containerId": "abc123def456",
  "url": "http://localhost:3001",
  "status": "starting"
}
```

#### POST `/exec/stop`
**Purpose**: Stop project container

**Input JSON**:
```json
{
  "sessionId": "68a2ab2404c0d9f324ed5a9e"
}
```

**Output JSON**:
```json
{
  "success": true,
  "message": "Container stopped successfully"
}
```

### 8️⃣ Session Endpoints

#### GET `/projects/:projectId/session/status`
**Purpose**: Get session status

**Output JSON**:
```json
{
  "success": true,
  "session": {
    "status": "running",
    "url": "http://localhost:3001",
    "logs": ["Container started", "Server running on port 3000"],
    "startedAt": "2024-01-15T10:30:00.000Z"
  }
}
```

---

## 🎨 User Flows & Pages

### 1️⃣ Authentication Flow

#### Login Page (`/login`)
- **Purpose**: User authentication
- **Components**: 
  - Email/Password form
  - "Sign up" link
  - "Forgot password" link (optional)
- **Flow**: Login → Store JWT → Redirect to Dashboard

#### Signup Page (`/signup`)
- **Purpose**: New user registration
- **Components**:
  - Username/Email/Password form
  - "Already have account?" link
- **Flow**: Signup → Auto-login → Redirect to Dashboard

### 2️⃣ Dashboard Flow

#### Dashboard Page (`/dashboard`)
- **Purpose**: Project overview and management
- **Components**:
  - User profile section
  - "Create New Project" button
  - Project cards grid/list
  - Project status indicators
- **Features**:
  - Filter projects by status
  - Search projects
  - Delete projects
  - View project details

### 3️⃣ Project Creation Flow

#### Project Creation Modal/Page
- **Purpose**: Initialize new project
- **Components**:
  - Project name input
  - Project idea description textarea
  - "Create Project" button
- **Flow**: Create → Redirect to Planning

### 4️⃣ Planning Flow

#### Planning Page (`/projects/:id/planning`)
- **Purpose**: AI-guided project planning
- **Components**:
  - Project overview
  - AI clarification questions
  - Answer input forms
  - "Submit Answers" button
- **Flow**: Answer questions → Generate blueprint → Redirect to Blueprint

### 5️⃣ Blueprint Flow

#### Blueprint Page (`/projects/:id/blueprint`)
- **Purpose**: Review and approve project blueprint
- **Components**:
  - Blueprint content display
  - Tech stack visualization
  - Architecture diagrams (Mermaid)
  - "Approve" / "Request Changes" buttons
- **Flow**: Review → Approve → Generate HLD/LLD → Redirect to Design

### 6️⃣ Design Flow

#### Design Page (`/projects/:id/design`)
- **Purpose**: Review system design documents
- **Components**:
  - HLD content with diagrams
  - LLD content with API specs
  - Interactive Mermaid diagrams
  - "Generate Code" button
- **Flow**: Review → Generate code → Redirect to Workspace

### 7️⃣ Workspace Flow

#### Workspace Page (`/projects/:id/workspace`)
- **Purpose**: Main IDE interface
- **Layout**: VS Code-like interface
- **Components**:
  - **Sidebar**: File explorer, project info
  - **Main Area**: Code editor, terminal
  - **Topbar**: Project name, user info, actions
- **Features**:
  - File tree navigation
  - Monaco editor integration
  - Real-time terminal
  - Project execution controls

### 8️⃣ File Explorer

#### File Tree Component
- **Purpose**: Navigate project files
- **Features**:
  - Expandable folder structure
  - File icons by type
  - Right-click context menu
  - Create new file/folder
  - Delete files/folders

### 9️⃣ Code Editor

#### Monaco Editor Integration
- **Purpose**: Code editing with IDE features
- **Features**:
  - Syntax highlighting
  - Auto-completion
  - Error detection
  - Multi-file tabs
  - Find/replace
  - Code formatting

### 🔟 Terminal & Execution

#### Terminal Component
- **Purpose**: View project execution logs
- **Features**:
  - Real-time log streaming
  - "Start Project" button
  - "Stop Project" button
  - Log filtering
  - Copy logs

---

## 🎨 Design System & UI Kit

### Framework & Styling
```json
{
  "framework": "React 18+ with TypeScript",
  "bundler": "Vite",
  "styling": "Tailwind CSS",
  "icons": "Lucide React",
  "editor": "@monaco-editor/react",
  "terminal": "xterm.js",
  "charts": "Mermaid.js",
  "state": "Zustand"
}
```

### Color Palette
```css
/* Primary Colors */
--primary-50: #eff6ff;
--primary-500: #3b82f6;
--primary-600: #2563eb;
--primary-700: #1d4ed8;

/* Neutral Colors */
--gray-50: #f9fafb;
--gray-100: #f3f4f6;
--gray-200: #e5e7eb;
--gray-800: #1f2937;
--gray-900: #111827;

/* Status Colors */
--success: #10b981;
--warning: #f59e0b;
--error: #ef4444;
--info: #3b82f6;
```

### Typography
```css
/* Headings */
.heading-1 { @apply text-3xl font-bold text-gray-900; }
.heading-2 { @apply text-2xl font-semibold text-gray-800; }
.heading-3 { @apply text-xl font-medium text-gray-800; }

/* Body Text */
.body-large { @apply text-lg text-gray-700; }
.body-medium { @apply text-base text-gray-700; }
.body-small { @apply text-sm text-gray-600; }
```

### Component Library

#### Button Variants
```jsx
// Primary Button
<Button variant="primary" size="md">
  Create Project
</Button>

// Secondary Button
<Button variant="secondary" size="md">
  Cancel
</Button>

// Danger Button
<Button variant="danger" size="md">
  Delete Project
</Button>
```

#### Card Component
```jsx
<Card>
  <CardHeader>
    <CardTitle>Project Name</CardTitle>
    <CardDescription>Project description</CardDescription>
  </CardHeader>
  <CardContent>
    Content goes here
  </CardContent>
  <CardFooter>
    <Button>Action</Button>
  </CardFooter>
</Card>
```

#### Modal Component
```jsx
<Modal isOpen={isOpen} onClose={onClose}>
  <ModalHeader>Create New Project</ModalHeader>
  <ModalBody>
    <Form>
      <FormField label="Project Name">
        <Input placeholder="Enter project name" />
      </FormField>
    </Form>
  </ModalBody>
  <ModalFooter>
    <Button variant="secondary" onClick={onClose}>Cancel</Button>
    <Button variant="primary" onClick={onSubmit}>Create</Button>
  </ModalFooter>
</Modal>
```

### Layout Components

#### Sidebar Layout
```jsx
<SidebarLayout>
  <Sidebar>
    <FileExplorer projectId={projectId} />
    <ProjectInfo project={project} />
  </Sidebar>
  <MainContent>
    <CodeEditor />
    <Terminal />
  </MainContent>
</SidebarLayout>
```

#### Dashboard Layout
```jsx
<DashboardLayout>
  <Header>
    <UserProfile />
    <CreateProjectButton />
  </Header>
  <MainContent>
    <ProjectGrid projects={projects} />
  </MainContent>
</DashboardLayout>
```

---

## 🔐 Authentication & State Management

### JWT Authentication Flow
```javascript
// Store token in localStorage
const login = async (credentials) => {
  const response = await api.post('/auth/login', credentials);
  const { token, user } = response.data;
  
  localStorage.setItem('token', token);
  localStorage.setItem('user', JSON.stringify(user));
  
  // Update global state
  useAuthStore.setState({ user, isAuthenticated: true });
};

// Send token with requests
const apiClient = axios.create({
  baseURL: 'http://localhost:5000/api/v1',
  headers: {
    'Content-Type': 'application/json',
  },
});

apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});
```

### State Management with Zustand
```javascript
// Auth Store
const useAuthStore = create((set) => ({
  user: null,
  isAuthenticated: false,
  login: (userData) => set({ user: userData, isAuthenticated: true }),
  logout: () => {
    localStorage.removeItem('token');
    localStorage.removeItem('user');
    set({ user: null, isAuthenticated: false });
  },
}));

// Project Store
const useProjectStore = create((set, get) => ({
  projects: [],
  currentProject: null,
  loading: false,
  
  fetchProjects: async () => {
    set({ loading: true });
    try {
      const response = await apiClient.get('/projects');
      set({ projects: response.data.projects, loading: false });
    } catch (error) {
      set({ loading: false });
    }
  },
  
  setCurrentProject: (project) => set({ currentProject: project }),
}));

// File Store
const useFileStore = create((set) => ({
  files: [],
  currentFile: null,
  fileContent: '',
  
  loadFiles: async (projectId) => {
    const response = await apiClient.get(`/files/list?projectId=${projectId}`);
    set({ files: response.data.entries });
  },
  
  loadFileContent: async (projectId, filePath) => {
    const response = await apiClient.get(`/files/read?projectId=${projectId}&relativePath=${filePath}`);
    set({ currentFile: filePath, fileContent: response.data.content });
  },
}));
```

### Protected Routes
```jsx
const ProtectedRoute = ({ children }) => {
  const { isAuthenticated } = useAuthStore();
  const navigate = useNavigate();
  
  useEffect(() => {
    if (!isAuthenticated) {
      navigate('/login');
    }
  }, [isAuthenticated, navigate]);
  
  return isAuthenticated ? children : null;
};

// Usage
<Routes>
  <Route path="/login" element={<LoginPage />} />
  <Route path="/signup" element={<SignupPage />} />
  <Route path="/dashboard" element={
    <ProtectedRoute>
      <DashboardPage />
    </ProtectedRoute>
  } />
  <Route path="/projects/:id/*" element={
    <ProtectedRoute>
      <ProjectWorkspace />
    </ProtectedRoute>
  } />
</Routes>
```

---

## 🔌 WebSocket/Real-time Requirements

### WebSocket Connection
```javascript
// Connect to WebSocket server
const socket = io('http://localhost:5000', {
  auth: {
    token: localStorage.getItem('token')
  }
});

// Join project room
socket.emit('join-project', { projectId: '68a2ab2404c0d9f324ed5a9e' });
```

### Real-time Events

#### Log Streaming
```javascript
// Listen for log updates
socket.on('logs:update', (data) => {
  const { sessionId, logs } = data;
  // Update terminal with new logs
  terminal.write(logs);
});

// Example log data
{
  "sessionId": "68a2ab2404c0d9f324ed5a9e",
  "logs": "Server running on port 3000\nDatabase connected successfully\n"
}
```

#### Project Status Updates
```javascript
// Listen for project status changes
socket.on('project:status', (data) => {
  const { projectId, status, url } = data;
  // Update project status in UI
  useProjectStore.getState().updateProjectStatus(projectId, status);
});

// Example status data
{
  "projectId": "68a2ab2404c0d9f324ed5a9e",
  "status": "running",
  "url": "http://localhost:3001"
}
```

#### File Changes (Future)
```javascript
// Listen for file changes (collaboration)
socket.on('file:changed', (data) => {
  const { projectId, filePath, content, userId } = data;
  // Update file content if not current user
  if (userId !== currentUser.id) {
    useFileStore.getState().updateFileContent(filePath, content);
  }
});
```

### WebSocket Service
```javascript
class WebSocketService {
  constructor() {
    this.socket = null;
    this.listeners = new Map();
  }
  
  connect(token) {
    this.socket = io('http://localhost:5000', {
      auth: { token }
    });
    
    this.setupEventListeners();
  }
  
  setupEventListeners() {
    this.socket.on('logs:update', (data) => {
      this.emit('logs:update', data);
    });
    
    this.socket.on('project:status', (data) => {
      this.emit('project:status', data);
    });
    
    this.socket.on('file:changed', (data) => {
      this.emit('file:changed', data);
    });
  }
  
  joinProject(projectId) {
    this.socket.emit('join-project', { projectId });
  }
  
  leaveProject(projectId) {
    this.socket.emit('leave-project', { projectId });
  }
  
  on(event, callback) {
    if (!this.listeners.has(event)) {
      this.listeners.set(event, []);
    }
    this.listeners.get(event).push(callback);
  }
  
  emit(event, data) {
    const callbacks = this.listeners.get(event) || [];
    callbacks.forEach(callback => callback(data));
  }
  
  disconnect() {
    if (this.socket) {
      this.socket.disconnect();
      this.socket = null;
    }
  }
}

export const wsService = new WebSocketService();
```

---


## 📦 Expected Deliverables

### 1️⃣ Core Pages
- [ ] **Login Page** (`/login`)
- [ ] **Signup Page** (`/signup`)
- [ ] **Dashboard Page** (`/dashboard`)
- [ ] **Project Planning Page** (`/projects/:id/planning`)
- [ ] **Blueprint Page** (`/projects/:id/blueprint`)
- [ ] **Design Page** (`/projects/:id/design`)
- [ ] **Workspace Page** (`/projects/:id/workspace`)

### 2️⃣ Reusable Components
- [ ] **Navbar** - Top navigation with user info
- [ ] **Sidebar** - File explorer and project info
- [ ] **CodeEditor** - Monaco editor integration
- [ ] **Terminal** - xterm.js terminal component
- [ ] **FileTree** - File/folder navigation
- [ ] **Modal** - Reusable modal component
- [ ] **Button** - Styled button variants
- [ ] **Card** - Project and content cards
- [ ] **Form** - Form components with validation

### 3️⃣ Services & Utilities
- [ ] **API Client** - Axios-based API service
- [ ] **WebSocket Service** - Real-time communication
- [ ] **Auth Service** - JWT management
- [ ] **File Service** - File operations
- [ ] **Project Service** - Project management
- [ ] **Storage Service** - Local storage utilities

### 4️⃣ State Management
- [ ] **Auth Store** - User authentication state
- [ ] **Project Store** - Project data management
- [ ] **File Store** - File system state
- [ ] **UI Store** - UI state management

### 5️⃣ Integration Features
- [ ] **Backend API Integration** - All endpoints connected
- [ ] **JWT Authentication** - Token-based auth
- [ ] **Real-time Updates** - WebSocket integration
- [ ] **File Operations** - Read/write project files
- [ ] **Project Execution** - Start/stop containers
- [ ] **Responsive Design** - Mobile-friendly layout

---

## 🛠 Technical Stack

### Core Dependencies
```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.8.0",
    "typescript": "^5.0.0",
    "vite": "^4.0.0",
    "@vitejs/plugin-react": "^3.0.0",
    "tailwindcss": "^3.3.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "zustand": "^4.3.0",
    "axios": "^1.3.0",
    "socket.io-client": "^4.6.0",
    "@monaco-editor/react": "^4.5.0",
    "xterm": "^5.1.0",
    "xterm-addon-fit": "^0.7.0",
    "lucide-react": "^0.263.0",
    "mermaid": "^10.0.0",
    "clsx": "^1.2.0",
    "react-hook-form": "^7.43.0",
    "zod": "^3.20.0",
    "@hookform/resolvers": "^2.9.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@types/node": "^18.0.0",
    "eslint": "^8.0.0",
    "prettier": "^2.8.0"
  }
}
```

### Project Structure
```
client/
├── public/
│   ├── index.html
│   └── favicon.ico
├── src/
│   ├── components/
│   │   ├── ui/
│   │   │   ├── Button.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── Modal.tsx
│   │   │   └── Form.tsx
│   │   ├── layout/
│   │   │   ├── Navbar.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   └── Layout.tsx
│   │   ├── editor/
│   │   │   ├── CodeEditor.tsx
│   │   │   ├── FileTree.tsx
│   │   │   └── Terminal.tsx
│   │   └── workspace/
│   │       ├── ProjectCard.tsx
│   │       ├── BlueprintViewer.tsx
│   │       └── DesignViewer.tsx
│   ├── pages/
│   │   ├── LoginPage.tsx
│   │   ├── SignupPage.tsx
│   │   ├── DashboardPage.tsx
│   │   ├── PlanningPage.tsx
│   │   ├── BlueprintPage.tsx
│   │   ├── DesignPage.tsx
│   │   └── WorkspacePage.tsx
│   ├── stores/
│   │   ├── authStore.ts
│   │   ├── projectStore.ts
│   │   └── fileStore.ts
│   ├── services/
│   │   ├── api.ts
│   │   ├── websocket.ts
│   │   └── auth.ts
│   ├── utils/
│   │   ├── constants.ts
│   │   ├── helpers.ts
│   │   └── types.ts
│   ├── styles/
│   │   └── globals.css
│   ├── App.tsx
│   └── main.tsx
├── package.json
├── vite.config.ts
├── tailwind.config.js
├── tsconfig.json
└── README.md
```

---

## 📋 Development Guidelines

### Code Style
- Use TypeScript for type safety
- Follow ESLint and Prettier configurations
- Use functional components with hooks
- Implement proper error handling
- Add loading states for async operations

### Component Guidelines
- Keep components small and focused
- Use composition over inheritance
- Implement proper prop validation
- Add accessibility attributes
- Use semantic HTML elements

### State Management
- Use Zustand for global state
- Keep local state in components when possible
- Implement optimistic updates
- Handle loading and error states

### API Integration
- Use axios interceptors for auth
- Implement proper error handling
- Add request/response logging
- Use TypeScript interfaces for API types

### Testing Strategy
- Unit tests for utilities and services
- Component tests for UI components
- Integration tests for API calls
- E2E tests for critical user flows

### Performance Optimization
- Use React.memo for expensive components
- Implement code splitting with lazy loading
- Optimize bundle size with tree shaking
- Use proper caching strategies

### Security Considerations
- Validate all user inputs
- Sanitize data before rendering
- Implement proper CORS handling
- Use secure storage for sensitive data

---

## 🚀 Getting Started

### Prerequisites
- Node.js 18+
- npm or yarn
- Backend server running on localhost:5000

### Installation
```bash
# Clone the repository
git clone <repository-url>
cd client

# Install dependencies
npm install

# Start development server
npm run dev
```

### Environment Variables
```env
VITE_API_BASE_URL=http://localhost:5000/api/v1
VITE_WS_URL=http://localhost:5000
VITE_APP_NAME=VibeCoders IDE
```

### Available Scripts
```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "lint:fix": "eslint src --ext ts,tsx --fix",
    "format": "prettier --write src/**/*.{ts,tsx,css,md}"
  }
}
```

---

## 📞 Support & Resources

### Documentation Links
- [React Documentation](https://react.dev/)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Monaco Editor](https://microsoft.github.io/monaco-editor/)
- [Socket.IO Client](https://socket.io/docs/v4/client-api/)

### Development Tools
- [Vite](https://vitejs.dev/) - Fast build tool
- [Zustand](https://github.com/pmndrs/zustand) - State management
- [Lucide Icons](https://lucide.dev/) - Icon library
- [Mermaid](https://mermaid.js.org/) - Diagram generation

### Testing Tools
- [Vitest](https://vitest.dev/) - Unit testing
- [React Testing Library](https://testing-library.com/docs/react-testing-library/intro/) - Component testing
- [Playwright](https://playwright.dev/) - E2E testing

---

**🎯 This FSD provides a comprehensive guide for frontend developers to build the VibeCoders IDE interface. Follow the specifications, use the provided test data, and ensure all integrations work seamlessly with the backend API.**
